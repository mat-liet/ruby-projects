require 'pry'

class BubbleSort

    def bubble_sort(array)
        index = 0
        for i in array do
            for j in array - i do
                
            end
          end
        # if num > array[index + 1]
        #     array[index], array[index + 1] = array[index + 1], array[index]
        # end
    end
end